Index: gst-fluendo-mp3-0.10.7/decoder/flump3dec.c
===================================================================
--- gst-fluendo-mp3-0.10.7.orig/decoder/flump3dec.c
+++ gst-fluendo-mp3-0.10.7/decoder/flump3dec.c
@@ -741,6 +741,7 @@ flump3dec_total_bytes (FluMp3Dec *flump3
   gst_query_set_duration (query, GST_FORMAT_BYTES, -1);
 
   if (!gst_pad_query (peer, query)) {
+    gst_query_unref (query);
     gst_object_unref (peer);
     return FALSE;
   }
@@ -749,6 +750,7 @@ flump3dec_total_bytes (FluMp3Dec *flump3
   
   gst_query_parse_duration (query, NULL, total);
 
+  gst_query_unref (query);
   return TRUE;
 }
 
